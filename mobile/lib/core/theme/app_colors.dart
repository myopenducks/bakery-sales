import 'package:flutter/material.dart';

class AppColors {
  // Brand Palette
  static const Color surfaceLight = Color(0xFFDAD7CD); // light background / surfaces
  static const Color surfaceSecondary = Color(0xFFA3B18A); // secondary surfaces / chips / subtle borders
  static const Color primary = Color(0xFF588157); // primary actions / active controls
  static const Color primaryDark = Color(0xFF3A5A40); // secondary strong elements / appbar / headers
  static const Color textDark = Color(0xFF344E41); // dark text / dark UI elements

  // Semantic UI helpers
  static const Color background = Color(0xFFF4F3EF);
  static const Color cardBackground = Colors.white;
  static const Color error = Color(0xFFBA1A1A);
  static const Color success = Color(0xFF386641);
  static const Color textMuted = Color(0xFF6B705C);
}
